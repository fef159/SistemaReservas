<UserControl Style="{StaticResource {x:Type UserControl}}" x:Class="SistemaReservas.Views.AulasView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:b="clr-namespace:SistemaReservas.Behaviors" b:ViewCommands.LoadedCommand="{Binding ActualizarCommand}">
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
  <StackPanel Margin="0,0,0,26"><TextBlock Text="ESPACIOS / DIRECTORIO" Style="{StaticResource Eyebrow}" /><TextBlock Text="Aulas" Style="{StaticResource PageTitle}" /><TextBlock Text="Encuentra el espacio adecuado para tu próxima actividad." Style="{StaticResource Subtitle}" /></StackPanel>
  <Border Grid.Row="1" Style="{StaticResource Card}" Padding="20" Margin="0,0,0,20">
   <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
    <StackPanel><TextBlock Text="Buscar por nombre de aula" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,8" /><TextBox x:Name="txtBuscar" AutomationProperties.Name="Buscar por nombre de aula" ToolTip="Escribe el nombre completo o parte del nombre." Text="{Binding Busqueda, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
    <Button Grid.Column="1" Content="Buscar" Margin="12,24,0,0" Command="{Binding BuscarCommand}" />
    <Button x:Name="ActualizarButton" Grid.Column="2" Content="↻  Actualizar" Margin="12,24,0,0" Style="{StaticResource SecondaryButton}" Command="{Binding ActualizarCommand}" />
   </Grid>
  </Border>
  <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="66" /><RowDefinition Height="*" /><RowDefinition Height="48" /></Grid.RowDefinitions>
    <Grid Margin="22,0"><TextBlock Text="Directorio de aulas" FontSize="17" FontWeight="SemiBold" VerticalAlignment="Center" /><TextBlock x:Name="CountLabel" Text="{Binding Resumen}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource Muted}" FontSize="12" /></Grid>
    <DataGrid x:Name="dgAulas" Grid.Row="1" Margin="1,0" AutomationProperties.Name="Directorio de aulas" ItemsSource="{Binding AulasDataTable.DefaultView}">
     <DataGrid.Columns><DataGridTextColumn ElementStyle="{StaticResource TableText}" Header="CÓDIGO" Binding="{Binding AulaId, StringFormat=A-{0:000}}" Width="120" /><DataGridTextColumn ElementStyle="{StaticResource TableText}" Header="NOMBRE DEL AULA" Binding="{Binding Nombre}" Width="*" /><DataGridTextColumn ElementStyle="{StaticResource TableText}" Header="CAPACIDAD" Binding="{Binding Capacidad, StringFormat={}{0} personas}" Width="180" /></DataGrid.Columns>
    </DataGrid>
    <TextBlock x:Name="EmptyLabel" Grid.Row="1" Text="{Binding MensajeVacio}" Foreground="{StaticResource Muted}" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="430" Margin="24" Visibility="{Binding MostrarMensaje, Converter={StaticResource BooleanToVisibility}}" />
    <Border Grid.Row="2" BorderBrush="{StaticResource Line}" BorderThickness="0,1,0,0" Padding="22,0"><TextBlock x:Name="StatusLabel" Text="{Binding Estado}" VerticalAlignment="Center" Foreground="{StaticResource Muted}" FontSize="12" TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite" /></Border>
   </Grid>
  </Border>
 </Grid>
</UserControl>
